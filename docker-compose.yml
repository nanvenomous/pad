services:
  starter:
    ports:
      - "4000:4000"
    build:
      dockerfile: ./Dockerfile.dev
    environment:
      PAD_NOTES_DIR: /tmp/pad
    volumes:
      - ./:/app
      - /app/tmp
      - ./tmp/pad:/tmp/pad
    networks:
      - net

networks:
  net:
    driver: bridge
